enabledHttpsBackendProtocol: false
enabledSslRedirect: false

namespace: default 

ingress:
  host: myapps.ypcloud.com
  path: /fbuilder/

host:
  path:
    motebus: /var/motebus/
    flowbot: /var/motebus/flow/

container:
    einame: fbuilder-exmaple
    motebusGateway: 127.0.0.1
    dc: dc
    userMode: "single"
    port: 18880
    storageMode: "cloud"


mbstack:
  upstream: gke.ypcloud.com


replicaCount: 1

image:
  repository:
    motebus: motebus/motebus:v2.4.8
    fbuilder: motebus/fbuilder:latest
  pullPolicy: Always

service:
  type: ClusterIP
  protocol: TCP
  port: 80

nodeSelector: {}